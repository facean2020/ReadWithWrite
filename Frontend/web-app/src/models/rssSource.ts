export interface RssSource {
    id: string;
    url: string;
    name: string;
    description?: string;
}

export interface RecommendedRssSource {
    id: string;
    url: string;
    name: string;
    description: string;
}
